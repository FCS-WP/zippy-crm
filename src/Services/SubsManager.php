<?php
namespace ZippyCrm\Services;

defined( 'ABSPATH' ) || exit;

final class SubsManager {
	// TODO: render_optin_field(), save_optin_preference($user_id, $data), update_preferences($user_id, $prefs).
}
